# front-end-developer Role (Portable)

See the full persona description in `agents/personas/front-end-developer.md` and the Grok-native definition in `.grok/personas/front-end-developer.toml`.

This role is suitable as a `subagent_type` when you want a subagent that defaults to the front-end-developer behavioral overlay and read-write capability on the UI parts of the monorepo.
