{
  "extends": ["//"],
  "tasks": {
    "tsgo:build": {
      "inputs": ["tsconfig*.json"],
      "outputs": ["**/*.tsbuildinfo"]
    },
    "tsgo:noEmit": {
      "inputs": ["tsconfig*.json", "**/*.ts", "**/*.tsx"]
    }
  }
}
