# Backend Expert Role (Portable)

Specialized agent for the Hono + Apollo GraphQL backend in apps/api.

Key strengths:
- GraphQL schema and resolver design (using graphql-tag + apollo-server-integration-hono)
- Azure Functions v4 compatibility via the hono adapter
- Keeping the API clean while serving both staff and public frontends
- Working with the in-memory demo store or planning real data layers

See the detailed version in `.grok/agents/backend-expert.md`.